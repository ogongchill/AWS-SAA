
# AWS Identity And Access Management(IAM)

>*AWS 리소스에 누가 “인증(누구)”하고, 무엇을 “허가(무엇을)” 할 수 있는지 제어하는 서비스.*

```
개발팀 사용자에게 S3 읽기 권한만 주고, 운영팀 사용자에게 EC2 시작/중단 권한 주기
```

---

# AWS IAM Identity Center

>*여러 AWS 계정이나 애플리케이션에 대한 중앙 인증·접근 관리(SSO) 역할을 하는 서비스.*

```
회사 내부 직원이 여러 AWS 계정에 하나의 로그인으로 접근하도록 설정
```

___

# Amazon Cognito

>*웹/모바일 애플리케이션 사용자의 인증 및 사용자 디렉터리 기능을 제공하는 서비스.*

```
모바일 앱에서 사용자 로그인/회원가입 기능 구현하고, 로그인 사용자의 JWT 토큰 기반 인증 사용
```

---

# Amazon Verified Permissions

>*애플리케이션 내에서 행동 허가(authorization)를 정책 기반으로 세밀히 제어할 수 있게 해주는 서비스.*

```
SaaS 애플리케이션에서 사용자의 역할, 리소스 속성, 외부 조건 등을 기반으로 “이 사용자에게 이 리소스 접근 허용인가?” 판단 구현
```

---

# AWS Directory Service


>*기존 내부 디렉터리(예: Microsoft Active Directory)를 AWS에 연동하거나 AWS에서 관리형 디렉터리 제공하는 서비스.*

```
온프레미스 AD 사용자를 AWS 리소스 접근용으로 동기화해서 사용
```

---

# AWS Resource Access Manager

>*여러 AWS 계정이나 조직 단위(OU)에서 리소스를 공유할 수 있게 해주는 서비스.*

```
조직 내 여러 계정에서 하나의 Transit Gateway 또는 VPC 서브넷을 공통으로 사용하도록 공유 설정
```

---

# AWS Organizations

>*다수의 AWS 계정을 체계적으로 관리하고, 조직 단위 정책(SCP) 등을 적용할 수 있게 해주는 서비스.*

```
회사가 여러 사업부별로 AWS 계정을 갖고 있을 때, 중앙에서 예산 제어·보안 정책을 조직 단위로 적용
```

---

# Amazaon GuardDuty

>*AWS 환경에서 로그/이벤트 데이터를 계속 분석해 위협 탐지(threat detection)를 수행하는 서비스.*

```
계정 내에서 이상 로그인 패턴, 악성 IP 접속 등의 이상 행동이 있는지 자동 탐지하고 경고
```

---

# Amazon Inspector

>*워크로드(EC2 인스턴스, 컨테이너 이미지, Lambda 등)를 대상으로 취약점 스캔 및 네트워크 노출 검사하는 서비스.*


```
새로 배포한 EC2 이미지에 대해 알려진 CVE(취약점) 여부를 검사하고 보안 리포트 생성
```

---

# AWS Security Hub

>*여러 보안 서비스의 결과(발견된 위협, 상태 등)를 통합하고 대시보드 형태로 보여주는 “보안 상태 중앙화” 서비스.*

```
GuardDuty, Inspector 등의 경고를 모아서 한곳에서 우선순위별로 보고하고 대응 흐름 관리
```

---

# Amazon Security Lake

>*보안 로그·이벤트 데이터를 대규모로 수집·저장하고 분석할 수 있게 해주는 데이터 레이크형 보안 분석 플랫폼.*

```
여러 계정/리전에서 수집된 VPC Flow Logs, CloudTrail 로그 등을 중앙 저장해 이상탐지 분석 가능
```

---

# Amazon Detective

>*보안 사고 및 위협 탐지 이후 “원인 조사”에 특화된 서비스로, 그래프 분석 등을 통해 이상행동 관계 파악 가능.*

```
GuardDuty에서 경고가 나왔을 때, 누가 언제 무슨 리소스에 접근했는지 흐름을 시각화해서 조사
```

---

# AWS Config

>*AWS 리소스의 설정(configuration) 변경을 감지하고 기록하며 규정 준수(compliance) 체크를 지원하는 서비스.*

```
S3 버킷이 언제부터 공개로 변경됐는지 추적하고 이전 상태로 되돌릴 수 있도록 기록
```


---

# Amazon CloudWatch


>*시스템 및 리소스의 로그·메트릭을 수집하고 알람·대시보드를 제공하는 모니터링 서비스.*

```
EC2 CPU 사용률이 일정 수준 이상일 때 알람 트리거해서 자동 확장 실행
```

---

# AWS CloudTrail

>*AWS 계정 내에서 발생한 API 호출 및 계정 활동을 기록하는 서비스로, 보안 감사(audit)나 추후 조사(forensics)에 필수.*

```
누가 S3 객체를 삭제했는지 등의 이벤트를 시간·사용자까지 로그로 남김
```

---

# AWS IoT Device Defender

>*IoT 디바이스 및 그 연결 환경을 대상으로 보안 이상행동을 탐지하고 정책을 적용하는 서비스.*

```
수천 개의 IoT 센서가 이상 통신을 하거나 비인가 접근 시 알림 발생
```

---

# AWS Elastic Disaster REcovery

>*AWS에서 재해복구(Disaster Recovery) 환경을 구성하고 복구 절차를 자동화하는 서비스.*

```
주 리전 장애 시 다른 리전으로 워크로드 자동 복구하여 다운타임 최소화
```

---

# AWS Firewall Manager


>*조직 전체에서 보안 방화벽 규칙(WAF, Shield 등)을 중앙에서 일괄 적용·관리하는 서비스.*

```
여러 계정/리전에 걸쳐 웹 애플리케이션 방화벽 정책을 통일해서 배포
```

---

# AWS Network Firewall


>VPC 네트워크 트래픽을 검사하고 사용자 정의 룰을 적용할 수 있는 L3/L4 방화벽 서비스.

```
프라이빗 서브넷간 트래픽을 필터링하거나, 알려진 악성 IP를 차단하는 네트워크 방화벽 구축
```

---

# AWS Shield


>*DDoS(분산 서비스 거부) 공격으로부터 애플리케이션을 보호하는 서비스.*

```
웹사이트에 대규모 트래픽 공격이 들어올 때 자동으로 보호 모드 작동
```

---

# AWS Verified Access

>*제로 트러스트(Zero Trust) 접근을 지원하는 서비스로서, 사용자나 디바이스의 접속을 제어하고 보안 액세스를 수행.*

```
원격근무 사용자가 회사 내부 서비스에 접속하려 할 때, 디바이스 상태·사용자 권한을 확인하고 접속 허용
```

---

# AWS 웹 어플리케이션 방화벽(WAF)

>웹 애플리케이션에 대한 HTTP/HTTPS 요청을 필터링하고 웹 공격(SQL 인젝션, XSS 등)을 방지하는 방화벽 서비스.

```
특정 URL 경로로 들어오는 악성 필터링 패턴을 설정해 공격 차단
```

---
# Amazon Route53 Resolver DNS 방화벽

>*DNS 레벨에서 악성도메인 차단 또는 허용하는 방화벽 기능을 제공하는 서비스.*

```
내부 네트워크에서 알려진 악성 도메인을 DNS 쿼리 단계에서 차단
```

---

# Amazon Macie

>*S3 버킷 등에 저장된 데이터 중 민감정보(PII 등)를 자동으로 탐지하고 분류하는 서비스.*

```
고객 개인정보가 포함된 파일이 S3에 저장됐을 때 자동으로 탐지하고 알림
```

---

# AWS Key Management Service(AWS KMS)

>*암호화 키를 생성·관리하고 AWS 리소스의 데이터 암호화를 간편히 지원하는 서비스.*

```
S3 버킷이나 EBS 볼륨을 KMS 키로 암호화해 저장
```

---

# AWS CloudHSM

>*하드웨어 보안 모듈(HSM) 기반으로 키를 저장하고 고보안 키관리 및 암호화 작업을 수행하는 서비스.*

```
금융권 등에서 자체적으로 강력한 키보관이 필요한 경우 사용
```

---

# AWS Certificate Manager (ACM)

>*SSL/TLS 인증서를 생성·관리하고 AWS 서비스(ELB, CloudFront 등)에 손쉽게 붙일 수 있게 하는 서비스*

```
웹사이트에 HTTPS를 적용할 때 ACM으로 인증서 발급 후 ALB에 붙여서 보안 연결 설정
```

---

# AWS Payment Cryptography

>카드 결제 정보 등 금융 트랜잭션 관련 암호화 및 키 관리를 위한 전문 서비스.

```
카드 결제 데이터를 암호화하고 결제 인증 키를 안전하게 관리
```

---

# AWS Prviate Certificate Authority

>*기업 내부용 인증서(자체 인증기관)를 AWS 환경에서 운영할 수 있게 해주는 서비스.*

```
내부 애플리케이션 서버 간 TLS 통신을 위해 사설 인증서 발급 및 관리
```

---

# AWS Secretes Manager

>*데이터베이스 암호, API 키 등 비밀정보를 안전하게 저장하고 자동으로 교체할 수 있게 지원하는 서비스.*

```
애플리케이션이 DB 암호를 직접 코드에 저장하지 않고 Secrets Manager에서 가져오도록 구성
```

---

# AWS Artifact 

>*AWS 컴플라이언스 보고서 및 보증 문서(Certifications, Audit Reports)를 다운로드할 수 있게 해주는 서비스.*

```
기업이 보안감사 준비 시 AWS의 ISO/PCI 등의 인증서를 열람하고 참고
```

---

# AWS Audit Manager

>*자동으로 보안 감사 활동을 준비하고 실행할 수 있게 해주는 서비스로, 규정 준수 체크리스트 제공 및 증적 수집 지원.*

```
금융권에서 규제준수 보고를 위해 사용하며, 여러 계정의 보안 상태를 감사할 때 유용
```

---
